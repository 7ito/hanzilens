# Environment variables for HanziLens backend
# Copy to .env and fill in values

# Server port (default: 5000)
PORT=5000

# Xiaomi MiMo API - required for /parse endpoint
# Get your API key from the Xiaomi MiMo console
MIMO_API_KEY=

# Model to use for text parsing
# Recommended: mimo-v2-flash
MIMO_MODEL=mimo-v2-flash

# OpenRouter API - optional
# Used for:
# - Image parsing OCR stage (requires OPENROUTER_API_KEY + OPENROUTER_VISION_MODEL)
# - Development-only /eval routes (requires OPENROUTER_API_KEY + OPENROUTER_MODEL)
# Get your API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model to use for /eval/parse (development only)
OPENROUTER_MODEL=

# Vision model for image OCR parsing (must support vision/images)
# Recommended options:
# - openai/gpt-4o (excellent OCR, fast)
# - anthropic/claude-sonnet-4 (great at Chinese understanding)
# - google/gemini-2.0-flash-001 (fast, cost-effective)
OPENROUTER_VISION_MODEL=

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
