# Environment variables for HanziLens backend
# Copy to .env and fill in values

# Server port (default: 5000)
PORT=5000

# OpenRouter API - required for /parse endpoint
# Get your API key at: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Model to use for text parsing. Recommended options:
# - anthropic/claude-3.5-sonnet (high quality, ~$3/M tokens)
# - google/gemini-flash-1.5 (fast, cheap ~$0.075/M tokens)  
# - qwen/qwen-2.5-72b-instruct (good for Chinese, ~$0.30/M tokens)
# See all models at: https://openrouter.ai/models
OPENROUTER_MODEL=

# Vision model for image OCR parsing (must support vision/images)
# Recommended options:
# - openai/gpt-4o (excellent OCR, fast)
# - anthropic/claude-sonnet-4 (great at Chinese understanding)
# - google/gemini-2.0-flash-001 (fast, cost-effective)
OPENROUTER_VISION_MODEL=

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Trust proxy setting for correct client IP detection (affects rate limiting)
# Set to number of proxy hops (e.g., 1 for single reverse proxy like Caddy)
# Set to "true" to trust all proxies
# Leave unset if running without a reverse proxy
# TRUST_PROXY=1

# Enable eval endpoint for model benchmarking (default: disabled)
# WARNING: This endpoint has no authentication and allows arbitrary model overrides
# ENABLE_EVAL=true
